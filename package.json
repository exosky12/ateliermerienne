{
	"type": "module",
	"private": "true",
	"license": "UNLICENSED",
	"scripts": {
		"dev": "pnpm -r dev",
		"docker:up": "docker compose up -d",
		"docker:down": "docker compose down",
		"lint": "pnpm -r lint",
		"format": "pnpm -r format"
	},
	"workspaces": {
		"packages": [
			"apps/*",
			"packages/*"
		]
	}
}
